import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.yellowAccent;
  static const accent = Color.fromARGB(255, 4, 6, 130);
  static const caution = Colors.redAccent;
  static const secondary = Colors.white;
}
